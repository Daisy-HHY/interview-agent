# 0.2.4 稳定性与面试评价优化：实际执行记录

**执行日期：** 2026-09-05  
**基础提交：** `1fdea0c`，中文提交信息“完善 0.2.3 运行诊断、压缩评测与动态预算”。

## 已完成的代码工作

- 历史裁剪按完整工具调用组处理；模型请求使用副本，原始业务历史继续用于保存和报告。
- 会话保存改为同目录临时文件写入、刷新并原子替换；损坏存档显式报错并保留原文件。
- VS Code API Key 使用 SecretStorage，兼容旧设置迁移；Webview 不回传密钥内容。
- 文件读取、搜索和目录列举统一工作区及敏感路径边界，并增加文件字节、行数和结果数量上限。
- benchmark 隔离会话存储，修正缺失样本分母、预算耗尽状态、runtime 分组和实际用量记录。
- 报告明确区分 JD、候选人自述、回答原文、历史反馈和通用建议；证据不足时不生成能力结论。
- 启动、停止、配置切换和 SecretStorage 异常路径增加状态保护与用户可见错误提示。

## 自动化验证

在项目根目录执行：

```powershell
.\.venv\Scripts\python.exe -m pytest -q
.\.venv\Scripts\python.exe -m ruff check agent
git diff --check
```

在 `vscode-extension` 目录执行：

```powershell
npm test
.\node_modules\.bin\tsc.cmd -p ./ --noEmit
npm run compile
npm run package
```

Python 全量测试、Ruff、扩展 120 个测试、TypeScript 检查和编译均通过；fake benchmark 已生成 baseline/compaction 样本及汇总，预算耗尽和缺失样本统计用例通过。`npm run package` 也已通过，生成 `vscode-extension/interview-agent-0.2.0.vsix`；该产物仍沿用现有 0.2.0 版本号，本轮没有擅自升级版本字段或发布。

## 验收矩阵

后续按用户要求生成正式版本号的本地安装包：扩展 manifest 和锁文件已同步至 `0.2.4`，重新执行 `npm run package` 成功。产物为 [interview-agent-0.2.4.vsix](../../../vscode-extension/interview-agent-0.2.4.vsix)。已检查 ZIP 完整性、包内双版本声明，并逐文件比对打包的 Python 源码与编译资源，结果一致。安装验收：VS Code 扩展菜单选择“从 VSIX 安装”，选择该文件并重新加载窗口；扩展详情显示 `0.2.4`，打开 Interview Agent 后能够正常开始会话。实际安装与交互仍由本机 VS Code 验收确认。

| 范围 | 当前结果 | 可复验标准 |
|---|---|---|
| 历史与工具配对 | 自动化通过 | 每个 tool result 都有对应调用，低预算不拆组 |
| 会话保存恢复 | 自动化通过 | 写入失败旧文件仍可读，坏文件有明确错误 |
| 密钥存储 | 自动化通过 | 普通设置不保存新密钥，切换 endpoint 后读取正确 |
| 文件工具边界 | 自动化通过 | 敏感路径拒绝，搜索最多 20 条，读取受 64 KiB/200 行限制 |
| benchmark | 自动化和 fake benchmark 通过 | 缺失不进入分母，预算耗尽不计自然完成，样本不复用旧会话 |
| 报告 | 自动化通过 | 摘录带来源，未考察写证据不足，不冒充模型评分 |
| VS Code 实机 | 未执行 | 打开扩展后完成开始、停止、继续、恢复和导出，界面无重复消息且最终不忙碌 |
| 真实模型 | 未执行 | 固定样本分别跑 native/Pi，核对追问依据、token 用量、延迟和失败回退 |
| OCR | 未执行 | 提供可用 OCR 依赖和样本，确认识别结果及失败提示 |
| CI/发布 | 未执行 | CI 全部通过；检查 VSIX 内容、版本和 bundled-agent 与源码一致 |

外部验收项不能由本地单元测试替代，执行时应保存命令、环境、样本和结果。
